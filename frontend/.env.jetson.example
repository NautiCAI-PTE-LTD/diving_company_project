# Copy before building the UI on Jetson:
#   cp frontend/.env.jetson.example frontend/.env.production.local
#   npm run build
#
# Always show the login page when the UI opens (no auto-login from saved token).
VITE_FORCE_LOGIN_ON_START=1
